import React from "react";
import {Button} from "react-bootstrap";
import './Header.css';

export const Header = () => {
  return (
    <header>
      <Button variant="primary">Primary</Button>
    </header>
  );
};
